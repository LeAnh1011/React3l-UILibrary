export enum BORDER_TYPE {
  MATERIAL,
  BORDERED,
  FLOAT_LABEL,
}

export enum ValidateStatus {
  success = "success",
  warning = "warning",
  error = "error",
  validating = "validating",
}

export enum NUMBER_BUTTON {
  THREE = "three",
  TWO = "two",
}